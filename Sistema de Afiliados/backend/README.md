# Backend - Sistema de Afiliados

- Node.js + Express
- MySQL
- Firebase Auth

## Scripts
- `npm install` para instalar dependências
- `npm run dev` para rodar em modo desenvolvimento

Configure o arquivo `.env` com suas credenciais.
